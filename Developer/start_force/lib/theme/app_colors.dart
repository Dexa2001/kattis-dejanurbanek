import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const navy = Color(0xFF02112B);
  static const darkNavy = Color(0xFF061226);
  static const card = Color(0xFF111C2E);
  static const cardAlt = Color(0xFF0A1D44);
  static const cardDark = Color(0xFF10244F);
  static const deepPurple = Color(0xFF241D2D);

  // Brand / accent
  static const blue = Color(0xFF1976FF);
  static const teal = Color(0xFF00B8A9);
  static const cyan = Color(0xFF00A0C6);
  static const purple = Color(0xFF6C4DFF);
  static const orange = Color(0xFFFF8C1A);
  static const slate = Color(0xFF334155);

  // Status
  static const success = Colors.greenAccent;
  static const warning = Colors.orangeAccent;
  static const danger = Colors.redAccent;

  // Text / borders
  static const white = Colors.white;
  static const whiteMuted = Colors.white70;
  static const whiteSoft = Colors.white60;
  static const whiteFaint = Colors.white54;
  static const whiteSubtle = Colors.white30;
  static const border = Colors.white10;
  static const borderStrong = Colors.white24;
  static const borderLight = Colors.white38;

  // PDF-style/report colors used in UI sometimes
  static const reportPink = Color(0xFFE8C7C7);
  static const lightPanel = Color(0xFFEEF4FF);
}
